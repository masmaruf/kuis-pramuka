{
  "name": "kuis-pramuka",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && node -e \"process.env.NODE_TLS_REJECT_UNAUTHORIZED='0';require('child_process').execSync('npx wrangler pages deploy dist --project-name kuis-pramuka --commit-dirty=true',{stdio:'inherit'})\" ",
    "type-check": "tsc --noEmit",
    "type-check:watch": "tsc --noEmit --watch"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.7.0",
    "@tailwindcss/vite": "^4.3.1",
    "astro": "^6.4.6",
    "tailwindcss": "^4.3.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
